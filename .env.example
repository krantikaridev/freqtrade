# Optional later for live (DO NOT enable live until dry-run beats flat).
# Leave empty for dry-run public market data.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Freqtrade will read key/secret from config when you move off dry-run.
# Prefer injecting via env / secrets manager on the Oracle VM — never commit real values.
